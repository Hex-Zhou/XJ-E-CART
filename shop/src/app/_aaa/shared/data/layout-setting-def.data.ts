import { iiLayoutBase } from "./../models/layout-setting.model";
// * * * * * 所有資料 * * * * *
const base: iiLayoutBase = { dropcartType: "offcanvas", headerLayout: "classic" };
// * * * * * 匯出 * * * * *
export const layoutSettingDef = {
  base: base,
};
